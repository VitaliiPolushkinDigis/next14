import Auth from "@/components/forms/auth/Auth";
import React from "react";

const RegisterPage = () => {
  return (
    <div>
      <Auth type="register" />
    </div>
  );
};

export default RegisterPage;
